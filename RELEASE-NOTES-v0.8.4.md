# SIMS Doctor Site Diagnosis v0.8.4

## Fix
Doctor診断結果を再取り込みした際、すでにSBMへの登録完了を記録した案件がWriter / Creator / Merge案件として復活する問題を修正しました。

## Behavior
- 「SBMへの登録完了」を押した時点で、その引き渡しに含まれる確定案件を案件単位で記録します。
- 同一診断セッションで同じDoctor結果を再取り込みしても、記録済み案件はSBM引き渡しJSONから除外します。
- 後から新しく確定したCreator等の案件は別案件として引き渡せます。
- 既存の「現在の診断を終了」によるセッションリセット時は、この記録もクリアします。

## Regression test
1. 「ネイル 風水 就活」をCreatorへ引き渡し、SBMで処置完了・経過観察へ移行済みの状態を想定。
2. Diagnosisで同じDoctor精密診断結果を再取り込み。
3. 「SBM引き渡しを進める」で、同案件がCreator案件として再出力されないことを確認。
